***

description:
globs:
alwaysApply: true
-----------------

In order to check changes, you can run the following commands:

Run `bun run lint` to check code quality.

Run `bun run build` to ensure the code compile correctly.
