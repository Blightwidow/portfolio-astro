---
description: 
globs: 
alwaysApply: true
---
In order to check changes, you can run the following commands:

Run `yarn lint` to check code quality.

Run `yarn build` to ensure the code compile correctly.